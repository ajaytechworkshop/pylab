[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "medizintechnik-rag-app"
version = "1.0.0"
requires-python = ">=3.14"

[project.scripts]
medizintechnik = "__main__:main"

#[tool.setuptools.packages-find]
#where = ["."]
